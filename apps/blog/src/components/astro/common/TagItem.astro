---
interface Props {
  href?: string;
}

const { href } = Astro.props;
---
{href 
  ? (
    <a
      href={href}
      class="block px-2 py-1 rounded-2"
      bg="hover:primary-bg-hover"
      text="fg-muted hover:fg"
      transition-colors
    >
      <slot />
    </a>
  )
  : (
    <span
      class="px-2 py-1 rounded-2"
      bg="hover:bg-hover"
      text="fg-muted hover:fg"
      transition-colors
      cursor="default"
    >
      <slot />
    </span>
  )
}
